<h1>id:
<%= @message.id %>
　のメッセージ編集ページ</h1>
　
<%= form_for(@message) do |f|%>
  <%= f.label :content, 'メッセージ' %>
  <%= f.text_filed :content%>
  <%=f.submit '投稿' %>
<% end %>

<%= link_to '一覧へ戻る', message_path %>